name: 🛡️ OASIS CORE CI/CD (Sentinel)

on:
  push:
    branches: [ main, v2-bootloader ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    # 1. Descargar tu código
    - name: 📥 Checkout Repository
      uses: actions/checkout@v3

    # 2. Preparar Node.js (Cerebro)
    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        
    # 3. Preparar Python (Simulaciones)
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    # 4. Instalar Dependencias
    - name: 📦 Install Dependencies
      run: npm install

    # 5. AUDITORÍA DE SEGURIDAD (SAST Básico)
    - name: 🔒 Security Audit (NPM)
      run: npm audit --audit-level=high || true # No falla el build, pero avisa

    # 6. TEST UNITARIO: Física y Núcleo
    - name: ⚛️ Run Grand Unified Test
      run: npx tsx src/tests/GrandUnifiedTest.ts

    # 7. TEST SIMULACIÓN: Tráfico Phi
    - name: 🔮 Run Phi Simulation
      run: |
        cd simulation
        python verify_phi.py

    # 8. VERIFICACIÓN DE INTEGRIDAD
    - name: ✅ Build Verification
      run: echo "Oasis Core Build & Test Sequence Complete. System Nominal."
